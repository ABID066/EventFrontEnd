import React, {Fragment} from 'react';
import Login from "../components/auth/login.jsx";

const LoginPage = () => {
    return (
        <Fragment>
            <Login/>
        </Fragment>
    );
};

export default LoginPage;